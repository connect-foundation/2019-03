import styled from 'styled-components';

const PostCardLineWrapper = styled.div`
  /* Layout */
  display: flex;
  flex-direction: row;

  /* Box */
  margin-bottom: 28px;
`;

export default PostCardLineWrapper;
