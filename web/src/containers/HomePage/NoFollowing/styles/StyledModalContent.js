import styled from 'styled-components';

import { ModalContent } from '../../../../components/UserListModal/styles';

const StyledModalContent = styled(ModalContent)`
  /* Box */
  height: auto;
`;

export default StyledModalContent;
