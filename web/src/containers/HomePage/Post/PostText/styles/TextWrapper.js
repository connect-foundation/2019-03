import styled from 'styled-components';

const TextWrapper = styled.div`
  /* Layout */
  display: flex;
  align-items: center;

  /* Box */
  margin: 0px 15px;

  /* Font */
  font-size: 0.95rem;
`;

export default TextWrapper;
