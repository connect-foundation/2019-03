import styled from 'styled-components';

const IconWrapper = styled.span`
  /* Layout */
  display: inline-flex;
  align-items: center;

  /* Box */
  width: 40px;
  height: 40px;
`;

export default IconWrapper;
