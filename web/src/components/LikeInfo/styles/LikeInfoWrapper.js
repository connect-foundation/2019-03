import styled from 'styled-components';

const LikeInfoWrapper = styled.div`
  /* Layout */
  display: flex;
  align-items: center;

  /* Font */
  font-size: 14px;
  line-height: 25px;
`;

export default LikeInfoWrapper;
