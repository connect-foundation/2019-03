import styled from 'styled-components';

const ItemWrapper = styled.div`
  /* Layout */
  display: flex;
  flex-direction: row;
  align-items: center;

  width: 800px;
`;

export default ItemWrapper;
